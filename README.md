## NSERC bootstrapping

usage:
```
(copy over c51_2pt_hyperons.h5 -> dataFiles/c51_2pt_hyperons.h5)
pip install requirements.txt (if necessary)
ssh login_
python nserc_bootstrapping_script.py lambda_z (or whatever hadron)
```
